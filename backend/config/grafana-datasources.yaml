apiVersion: 1

datasources:
  - name: Tempo
    type: tempo
    access: proxy
    orgId: 1
    uid: tempo  # <--- กำหนด ID ให้ชัดเจน (เพื่อให้ Loki อ้างถึงได้)
    url: http://tempo:3200
    basicAuth: false
    isDefault: false
    version: 1
    editable: true

  - name: Loki
    type: loki
    access: proxy
    orgId: 1
    url: http://loki:3100
    basicAuth: false
    isDefault: true
    version: 1
    editable: true
    jsonData:
      derivedFields:
        # 👇 นี่คือส่วน "ทีเด็ด" ที่คุณต้องการ
        - datasourceUid: tempo  # อ้างอิงไปหา uid ของ Tempo ด้านบน
          matcherRegex: '"trace_id":"(\w+)"' # Regex จับค่า trace_id
          name: TraceID
          url: '$${__value.raw}' # Query ไปยัง Tempo